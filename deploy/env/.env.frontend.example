# Frontend build args — copy to deploy/env/.env.frontend (optional; compose uses root .env.staging / .env.production)

VITE_API_URL=https://staging.sante.example.com/api
VITE_TELECONSULT_PROVIDER=jitsi
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
